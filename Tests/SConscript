Import('env')
Import('ML')
Import('gtest')
Import('GTEST_LIBS')
Import('OTHER_LIBS')
penv = env.Clone()
sources = Glob('*.cpp')
penv.Append(LIBS= [OTHER_LIBS] + GTEST_LIBS)
Tests = penv.Program('Tests', sources + ML + gtest)
Return('Tests')
